# neovim config for cpp with the nvchad
C++ Starter config for NvChad
#### To avoid this error:
- ▶ # [14/14] spawn: python3 failed with exit code 1 and signal 0.
- Run the following command
```bash
sudo apt install -y python3-venv
```

### Installation
```bash
git clone https://github.com/TheSGJ/cpp-nvim-config.git ~/.config/nvim && nvim
```
